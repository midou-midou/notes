# 概念
垃圾回收，简称 GC（Garbage Collection）程序运行后会产生一些不**可达**的变量、对象等产生的内存空间，需要一种机制来管理这部分不用的内存空间

# 基本机制
- 标记清除法
- 引用计数法

# 标记清除法
## 简单描述
垃圾收集器会从“根”开始，**标记阶段**使用的活动对象，**清除阶段**则把没有标记的对象销毁，**根**是标记清除法的起始点，从“根”开始，依次遍历对象树 -> 标记活动对象 -> 清理没有标记的对象

